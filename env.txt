// activate environment (bash):
source env/Scripts/activate
// install package pillow for images (enabling ImageField)
// winpty in gitbash -> references win package for comm. with win console
// drop the database and delete migrations (if you migrated) before migrating 
// to custom user model!

